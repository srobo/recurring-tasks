summary:  Import the (final) teams data into the compstate

priority: major

component: Competition

milestone: $SRYYYY Competition Day

description: >-
     We need to run the teams import into the compstate as part of the
     setup so that it's got the latest team images.

     This import may also need re-running during the competition.

dependencies:
    - comp/website/compstate
