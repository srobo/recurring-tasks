summary: Update the website's teams data for $SRYYYY

priority: major

component: Website

milestone: $SRYYYY Kickstart

description: >-
    The website has a file (`resources/$YYYY/teams.json`) which contains
    data about all the teams competing each year. This file needs creating
    for the current year.

    There is a script (`generate-srweb.py`) to create it within the private
    `teams.git` repo.

dependencies:
    - kickstart/confirm-teams
